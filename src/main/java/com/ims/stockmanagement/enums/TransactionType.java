package com.ims.stockmanagement.enums;

public enum TransactionType {
    PURCHASE,   // Satın alma (stok artışı)
    SALE,       // Satış (stok azalışı)
    ADJUSTMENT  // Düzeltme (stok ayarlama)
}


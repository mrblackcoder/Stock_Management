spring.application.name=StockManagement

# Server Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/inventory_management_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
# CRITICAL SECURITY: NEVER commit JWT_SECRET to production!
# For production: Set JWT_SECRET environment variable (minimum 256 characters recommended)
# Generate secure secret: openssl rand -base64 64
jwt.secret=${JWT_SECRET:ThisIsADevelopmentSecretKeyOnlyForLocalTestingNeverUseInProductionPleaseSetEnvironmentVariable123456789}
jwt.expiration=900000
jwt.refresh.expiration=604800000

# Admin Configuration
# SECURITY: Set ADMIN_PASSWORD environment variable for production
# Never use default password in production environment
admin.default.password=${ADMIN_PASSWORD:Admin@123!Secure}

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.ims.stockmanagement=DEBUG

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.show-actuator=true

# Spring Boot Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true
management.health.livenessState.enabled=true
management.health.readinessState.enabled=true
management.metrics.export.prometheus.enabled=true
management.info.env.enabled=true

# Application Info (visible in /actuator/info)
info.app.name=Stock Management System
info.app.description=Enterprise-grade Inventory Management System
info.app.version=1.0.0
info.app.student.name=Mehmet Taha Boynikoğlu
info.app.student.id=212 125 10 34
info.app.university=Fatih Sultan Mehmet Vakıf Üniversitesi
info.app.course=Web Design and Programming

